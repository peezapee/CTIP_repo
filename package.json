{
  "name": "park-guide-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "web",
    "mobile",
    "server"
  ],
  "scripts": {
    "install:all": "npm install",
    "web": "npm run dev --workspace=web",
    "mobile": "npm run start --workspace=mobile",
    "backend": "npm run start --workspace=server"
  },
  "dependencies": {
    "multer": "^2.1.1"
  }
}
